#include <stdio.h>

int main() {
  int k, i, j;
  scanf("%d", &k);
  int numbers[k];
  for (i = 0; i < k; i++) {
    scanf("%d", &numbers[i]);
  }

  for (i = 0; i < 2; i++) {
    for (j = 0; j < k; j++) {
      printf("%d ", numbers[j]);
    }
    printf("\n");
  }

  return 0;
}
